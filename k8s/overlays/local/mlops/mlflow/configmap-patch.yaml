apiVersion: v1
kind: ConfigMap
metadata:
  name: mlflow-config
data:
  # Server proxy artifacts — client upload qua HTTP, server lưu vào PVC
  MLFLOW_ARTIFACTS_DESTINATION: "file:/mlflow/artifacts"
  MLFLOW_S3_ENDPOINT_URL: ""
  MLFLOW_S3_IGNORE_TLS: "false"
