apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mlops

resources:
  - ../../../../base/mlops/mlflow
  - mlflow-secret-local.yaml
  # File storage — không cần MinIO (đơn giản hơn cho local)
  - mlflow-artifacts-pvc.yaml

patches:
  - path: configmap-patch.yaml
  - path: patch-nodeport.yaml
  - path: patch-file-storage.yaml
