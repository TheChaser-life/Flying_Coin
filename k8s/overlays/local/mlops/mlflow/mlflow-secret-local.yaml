apiVersion: v1
kind: Secret
metadata:
  name: mlflow-secret
  namespace: mlops
type: Opaque
stringData:
  # Full PostgreSQL connection string (password embedded — acceptable for local dev)
  MLFLOW_BACKEND_STORE_URI: "postgresql://postgres:admintesting123@postgres-postgresql.default.svc.cluster.local:5432/mlflow"
  # MinIO credentials (S3-compatible)
  AWS_ACCESS_KEY_ID: "admin"
  AWS_SECRET_ACCESS_KEY: "admintesting123"
