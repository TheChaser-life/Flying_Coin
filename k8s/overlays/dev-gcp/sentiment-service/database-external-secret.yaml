apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sentiment-db-sync
  namespace: dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-storage
    kind: ClusterSecretStore
  target:
    name: sentiment-db-config
    creationPolicy: Owner
    template:
      data:
        DATABASE_URL: "{{ .db_url | toString | trim }}"
        NEWSAPI_KEY: "{{ .newsapi_key | toString | trim }}"
        RABBITMQ_URL: "{{ .rabbitmq_url | toString | trim }}"
        REDIS_URL: "{{ .redis_url | toString | trim }}"
  data:
    - secretKey: db_url
      remoteRef:
        key: market-data-db-url
    - secretKey: newsapi_key
      remoteRef:
        key: newsapi-key
    - secretKey: rabbitmq_url
      remoteRef:
        key: rabbitmq-url
    - secretKey: redis_url
      remoteRef:
        key: redis-url
