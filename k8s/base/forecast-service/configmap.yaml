apiVersion: v1
kind: ConfigMap
metadata:
  name: forecast-service-config
data:
  MLFLOW_TRACKING_URI: "http://mlflow-tracking-svc.mlops.svc.cluster.local:80"
  REDIS_URL: "redis://:redis@redis-master.database.svc.cluster.local:6379/0"
  # Artifact file storage — MLflow server serve qua HTTP, không cần S3 credentials
